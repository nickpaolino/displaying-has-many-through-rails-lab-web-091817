<ul>
<% @patients.each do |patient| %>
  <li>Name: <%= link_to patient.name, patient_path(patient) %><br></li>
  <%= "Number of Appointments: #{patient.appointments.count}" %>
  <% patient.doctors.each do |doctor| %>
    <%= doctor.name %><br>
  <% end %>
<% end %>
</ul>
